- `process_data`: Added tokenization and n-gram sequencing. Inputs and 
outputs are saved as `.pt` files
- `layers`: Tested `DotProductAttention` and `MultiHeadAttention` 
layers. Added `mask` in `DotProductAttention`